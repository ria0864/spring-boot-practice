import styled from 'styled-components';

const FormContainer = styled.div`
  display: flex;
  justify-content: center;
`;

const Form = styled.form`
  width: 300px;
`;

const FormItem = styled.div``;

export default { FormContainer, Form, FormItem };
